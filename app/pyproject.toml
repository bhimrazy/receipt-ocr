[project]
name = "receipt-ocr-api"
version = "0.1.0"
description = "FastAPI service for Receipt OCR"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "python-multipart",
    "pillow",
    "python-dotenv",
    "receipt-ocr>=0.3.0",
]